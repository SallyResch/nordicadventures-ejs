<div class="sidebar">
    <button class="sidebar_toggle">☰</button>
    <% if (pageName === "home") { %>
        <h4>All activities</h4>
        <% dataLists.forEach(list => { %>
        <% list.forEach(activity => { %>
          <a href="/<%= activity.name %>">
            <p class="sidebar_p"><%= activity.name %></p>
          </a>
        <% }) %>
    <% }) %>

    <% } %>

    <% if (pageName === "summer") { %>
        <h4>Summer activities</h4>
     <% dataList.forEach(activity => { %>
     <a href="/summer/<%= activity.name %>">
                <p class="sidebar_p"><%= activity.name %></p>
            </a>
     <% }) %>
     <% } %>

    <% if (pageName === "autumn") { %>
        <h4>Autumn activities</h4>
          <% dataList.forEach(activity => { %>
       <a href="/autumn/<%= activity.name %>">
                <p class="sidebar_p"><%= activity.name %></p>
            </a>
     <% }) %>
    <% } %>

    <% if (pageName === "winter") { %>
        <h4>Winter activities</h4>
    <% dataList.forEach(activity => { %>
       <a href="/winter/<%= activity.name %>">
                <p class="sidebar_p"><%= activity.name %></p>
            </a>
     <% }) %>
    <% } %>
</div>
